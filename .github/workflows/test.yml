name: Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  BUILD_TYPE: Release

jobs:
  build:
    runs-on: ubuntu-20.04
    name: "${{ github.workflow }}"

    steps:
    - name: Test environment
      shell: bash
      run: |
        echo "$USER"
        ls /home
